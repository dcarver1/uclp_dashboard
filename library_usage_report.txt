# R Directory Library Usage Report

## Libraries Called Within Each File in `R/`

*   **`add_field_notes.R`**: `dplyr`, `stringr`, `lubridate`, `tidyr`
*   **`apply_cleaning_filters.R`**: `dplyr`, `stringr`, `rlang`
*   **`apply_fdom_corrections.R`**: `dplyr`
*   **`apply_interpolation_missing_data.R`**: `dplyr`, `lubridate`, `zoo`, `rlang`
*   **`apply_low_pass_binomial_filter.R`**: `dplyr`, `data.table`, `purrr`, `rlang`
*   **`apply_timestep_median.R`**: `lubridate`, `dplyr`, `rlang`
*   **`apply_toc_model.R`**: `dplyr`, `cdssr`, `lubridate`, `tidyr`, `padr`, `stats`, `purrr`, `glue`, `xgboost` (implicitly via `predict()`)
*   **`apply_training_scale.R`**: `scales`, `dplyr`, `here`, `arrow`
*   **`create_rainbow_flow_plot.R`**: `dplyr`, `plotly`, `lubridate`, `tidyr`, `yaml`
*   **`home_module.R`**: `shiny`, `shinydashboard`, `shinyWidgets`, `arrow`, `later`, `htmltools`
*   **`plot_longitudinal.R`**: `ggplot2`, `ggridges`, `plotly`, `dplyr`
*   **`pull_contrail_api.R`**: `httr2`, `rvest`, `lubridate`, `purrr`, `dplyr`, `readr`, `stringr`
*   **`pull_wet_api.R`**: `httr2`, `purrr`, `lubridate`, `data.table`
*   **`read_ext.R`**: `readr`, `tools`, `arrow`, `readxl`

*(Note: `tidyverse` loaded in `global.R` encompasses `dplyr`, `stringr`, `lubridate`, `tidyr`, `purrr`, `ggplot2`, `readr`, and `readxl`)*

---

## Libraries Loaded by the Shiny App (`global.R`) but NOT Present in `R/` Files

The following libraries are loaded in `global.R` but are **not** actively used within any of the module/helper files inside the `R/` directory.

### Used elsewhere in the Shiny App (`ui.R` or `server.R`):
*   **`sf`**: Used in `server.R` (`st_as_sf`)
*   **`leaflet`**: Used heavily in `ui.R` and `server.R` (`leaflet`, `addTiles`, `addAwesomeMarkers`, etc.)
*   **`bslib`**: Used in `ui.R` and `server.R` (`card`, `layout_columns`)
*   **`shinycssloaders`**: Used in `ui.R` (`withSpinner`)

### Completely unused anywhere in the Shiny App (or currently commented out):
*   **`ross.wq.tools`**: Function calls like `load_mWater` are commented out in `server.R`.
*   **`janitor`**
*   **`broom`**
*   **`RcppRoll`**: Unused (the app uses `data.table::frollapply` instead).
*   **`trend`**
*   **`ggpubr`**
*   **`ggthemes`**
*   **`ggpmisc`**
*   **`httr`**: Unused (the app uses `httr2` exclusively).
*   **`devtools`**
*   **`shinymanager`**: Authentication logic is commented out in `ui.R` and `server.R`.
*   **`shinyTime`**
*   **`DT`**
